/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as graphql from "@nestjs/graphql";
import { GraphQLError } from "graphql";
import { isRecordNotFoundError } from "../../prisma.util";
import { MetaQueryPayload } from "../../util/MetaQueryPayload";
import { DeliveryPerson } from "./DeliveryPerson";
import { DeliveryPersonCountArgs } from "./DeliveryPersonCountArgs";
import { DeliveryPersonFindManyArgs } from "./DeliveryPersonFindManyArgs";
import { DeliveryPersonFindUniqueArgs } from "./DeliveryPersonFindUniqueArgs";
import { DeleteDeliveryPersonArgs } from "./DeleteDeliveryPersonArgs";
import { DeliveryPersonService } from "../deliveryPerson.service";
@graphql.Resolver(() => DeliveryPerson)
export class DeliveryPersonResolverBase {
  constructor(protected readonly service: DeliveryPersonService) {}

  async _deliveryPeopleMeta(
    @graphql.Args() args: DeliveryPersonCountArgs
  ): Promise<MetaQueryPayload> {
    const result = await this.service.count(args);
    return {
      count: result,
    };
  }

  @graphql.Query(() => [DeliveryPerson])
  async deliveryPeople(
    @graphql.Args() args: DeliveryPersonFindManyArgs
  ): Promise<DeliveryPerson[]> {
    return this.service.deliveryPeople(args);
  }

  @graphql.Query(() => DeliveryPerson, { nullable: true })
  async deliveryPerson(
    @graphql.Args() args: DeliveryPersonFindUniqueArgs
  ): Promise<DeliveryPerson | null> {
    const result = await this.service.deliveryPerson(args);
    if (result === null) {
      return null;
    }
    return result;
  }

  @graphql.Mutation(() => DeliveryPerson)
  async deleteDeliveryPerson(
    @graphql.Args() args: DeleteDeliveryPersonArgs
  ): Promise<DeliveryPerson | null> {
    try {
      return await this.service.deleteDeliveryPerson(args);
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new GraphQLError(
          `No resource was found for ${JSON.stringify(args.where)}`
        );
      }
      throw error;
    }
  }
}
